{
  "name": "@scriba/workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Scriba workspace",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm dev:web",
    "dev:desktop": "pnpm --filter @scriba/desktop dev",
    "dev:web": "pnpm --filter @scriba/web dev",
    "dev:api": "python3 -m uvicorn app.main:app --reload --app-dir apps/api",
    "dev:worker": "python3 -m celery -A worker.celery_app.celery_app worker --workdir workers/ai",
    "build": "pnpm build:web",
    "build:desktop": "pnpm --filter @scriba/desktop build",
    "build:web": "pnpm --filter @scriba/web build",
    "start:desktop": "pnpm --filter @scriba/desktop start"
  }
}

